To copy important files to AWS, run the following commands from the fortuna-backend-devnet-nodejs root directory.
Locally, run `npm run build`
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem -r ./dist ubuntu@54.90.242.166:/home/ubuntu/fortuna
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem -r ./prisma ubuntu@54.90.242.166:/home/ubuntu/fortuna/dist/src
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem ./package.json ubuntu@54.90.242.166:/home/ubuntu/fortuna
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem ./package-lock.json ubuntu@54.90.242.166:/home/ubuntu/fortuna
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem ./.env.production ubuntu@54.90.242.166:/home/ubuntu/fortuna/dist/src/.env
scp -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem ./ecosystem.config.js ubuntu@54.90.242.166:/home/ubuntu/fortuna/dist/src

To SSH into AWS:
ssh -i C:\Users\actam\Downloads\devnet-ec2-keypair.pem ubuntu@54.90.242.166

In AWS:
`cd fortuna`
`npm install --production`
`pm2 restart <app_name or app_id>`

For Database migrations, navigate to dist/src, and run::
	`npx prisma migrate deploy`
	If doing a database migration, will need to restart the process. Doing `pm2 restart <app_name or app_id>` won't work (this doesn't re-start the TS server)
	Delete the process `pm2 delete app_name_or_id`, and then `pm2 start ecosystem.config.js --env production` (must be done where the index.js file is)
	Run `npx prisma generate` in the src directory.
	Not sure which of the steps above make it work, or if it's just a time thing (db needs time to reset)

I am using the package pm2 to run the server on the instance (even after I close my connection)
Important pm2 commands:
1. pm2 list (shows pm2 instances)
2. pm2 stop app_name_or_id (stops a running pm2 process)
3. pm2 delete app_name_or_id (deletes a stopped pm2 process)
4. pm2 start ecosystem.config.js --env production (must be done where the index.js file is)
5. pm2 logs app_name_or_id (shows logs for a process)

Other:
- If getting this error:  `Error: @prisma/client did not initialize yet. Please run "prisma generate" and try to import it again.`, run:
	`npx prisma generate` in the src directory
